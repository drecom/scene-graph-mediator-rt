/**
 * Scene graph expression level.<br />
 * All converter must support these levels.
 */
const Level: {
  SUMMARY: number,
  DETAIL: number
} = Object.freeze({
  SUMMARY: 1,
  DETAIL: 2
});

export default Level;
